<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.breeze.mapper.FeedbackTypeMapper">
    <resultMap id="BaseResultMap" type="com.breeze.pojo.FeedbackType">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <sql id="Base_Column_List">
    id, `name`, create_time
  </sql>
    <select id="findById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM feedback_type
        WHERE id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteById" parameterType="java.lang.Integer">
    DELETE FROM feedback_type
    WHERE id = #{id,jdbcType=INTEGER}
  </delete>
    <insert id="add" parameterType="com.breeze.pojo.FeedbackType">
    INSERT INTO feedback_type (id, `name`, create_time
      )
    VALUES (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP}
      )
  </insert>
    <update id="update" parameterType="com.breeze.pojo.FeedbackType">
    UPDATE feedback_type
    SET `name` = #{name,jdbcType=VARCHAR},
      create_time = #{createTime,jdbcType=TIMESTAMP}
    WHERE id = #{id,jdbcType=INTEGER}
  </update>
    <select id="findAll" resultType="com.breeze.pojo.FeedbackType">
        SELECT
        <include refid="Base_Column_List"/>
        FROM feedback_type
    </select>
</mapper>