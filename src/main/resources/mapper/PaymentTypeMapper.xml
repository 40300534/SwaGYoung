<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.breeze.mapper.PaymentTypeMapper">
  <resultMap id="BaseResultMap" type="com.breeze.pojo.PaymentType">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
  </resultMap>
  <sql id="Base_Column_List">
    id, `name`
  </sql>
  <select id="findById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM payment_type
    WHERE id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteById" parameterType="java.lang.Integer">
    DELETE FROM payment_type
    WHERE id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="add" parameterType="com.breeze.pojo.PaymentType">
    INSERT INTO payment_type (id, `name`)
    VALUES (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR})
  </insert>
  <update id="update" parameterType="com.breeze.pojo.PaymentType">
    UPDATE payment_type
    SET `name` = #{name,jdbcType=VARCHAR}
    WHERE id = #{id,jdbcType=INTEGER}
  </update>
</mapper>