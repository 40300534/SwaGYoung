<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.breeze.mapper.OriginMapper">
    <resultMap id="BaseResultMap" type="com.breeze.pojo.ProductOrigin">
        <id column="ID" jdbcType="INTEGER" property="id"/>
        <result column="NAME" jdbcType="VARCHAR" property="name"/>
    </resultMap>
    <sql id="Base_Column_List">
    ID, `NAME`
  </sql>
    <select id="findById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM ORIGIN
        WHERE ID = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteById" parameterType="java.lang.Integer">
    DELETE FROM ORIGIN
    WHERE ID = #{id,jdbcType=INTEGER}
  </delete>
    <insert id="add" parameterType="com.breeze.pojo.ProductOrigin">
    INSERT INTO ORIGIN (ID, `NAME`)
    VALUES (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR})
  </insert>
    <update id="update" parameterType="com.breeze.pojo.ProductOrigin">
    UPDATE ORIGIN
    SET `NAME` = #{name,jdbcType=VARCHAR}
    WHERE ID = #{id,jdbcType=INTEGER}
  </update>

    <select id="findAll" resultType="com.breeze.pojo.ProductOrigin">
        SELECT
        <include refid="Base_Column_List" />
        FROM origin
    </select>
</mapper>